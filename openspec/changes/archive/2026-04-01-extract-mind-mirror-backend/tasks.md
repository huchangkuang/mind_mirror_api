## 1. 项目基线与迁移准备

- [x] 1.1 盘点同级 `mind_mirror` 后端模块、依赖、环境变量与数据库连接配置，形成迁移清单
- [x] 1.2 在 `mind_mirror_api` 初始化或校准 NestJS 工程骨架（AppModule、模块目录、配置加载）
- [x] 1.3 建立迁移分批策略（优先认证与用户核心链路）并定义每批验收标准

## 2. API 基础规范落地

- [x] 2.1 配置全局路由前缀与版本策略，确保所有接口统一在 `/api/v1`
- [x] 2.2 实现全局响应拦截器，将成功响应统一包装为 `code/message/data`
- [x] 2.3 实现全局异常过滤器，统一输出错误结构并对接错误码枚举

## 3. 请求校验与 DTO 规范

- [x] 3.1 在应用入口启用全局 ValidationPipe（transform/whitelist/forbidNonWhitelisted）
- [x] 3.2 为迁移接口补齐 DTO 并使用 `class-validator` 注解定义约束
- [x] 3.3 使用 `class-transformer` 处理常见参数类型转换并补充校验失败测试

## 4. 鉴权体系（JWT + Refresh Token）

- [x] 4.1 实现登录签发 Access Token + Refresh Token 的认证流程与配置
- [x] 4.2 实现刷新令牌接口（校验、轮换、撤销）并确定持久化策略（DB 或 Redis）
- [x] 4.3 为受保护接口接入鉴权守卫并完成无令牌/无效令牌场景测试
- [x] 4.4 实现登出失效机制，保证 Refresh Token 失效后不可复用

## 5. Prisma 数据访问标准化

- [x] 5.1 建立 Prisma schema 与现有数据库结构映射，补齐模型定义
- [x] 5.2 生成并执行首批 Prisma migrations，验证开发与测试环境一致性
- [x] 5.3 将迁移模块的数据访问统一到 Prisma Client，移除混用 ORM 路径
- [x] 5.4 对跨表写操作接入事务并补充失败回滚测试用例

## 6. 错误码体系与发布验收

- [x] 6.1 定义统一错误码枚举分段规则（参数/鉴权/资源/系统）并落地到共享模块
- [x] 6.2 建立异常到错误码映射工厂，覆盖框架异常与业务异常
- [x] 6.3 完成迁移前后关键接口契约回归与联调验证，确认响应结构与鉴权流程一致
- [x] 6.4 制定灰度发布与回滚步骤，监控错误码分布、鉴权失败率和关键接口时延
